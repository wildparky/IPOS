# 프로젝트 미디어 저장 구조

기준 프로젝트 데이터는 `~/.franklin/projects/projects.sqlite`에 저장합니다.
프로젝트 메타데이터, 노드, 연결은 별도 테이블로 관리합니다. SQLite WAL,
트랜잭션, 외래 키 및 쓰기 대기 시간을 사용하며, 서버 로컬 디스크에 둡니다.
네트워크 공유 폴더에 DB 파일을 놓고 여러 호스트의 백엔드가 직접 여는 방식이 아닙니다.
노트북과 데스크톱 브라우저는 하나의 Franklin 백엔드 API에 접속합니다.
미디어는 파일 내용의 해시를 이름으로 사용하여
`<id>/media/uploads/`와 `<id>/media/generated/`에 저장합니다.
노드 ID·연결·프롬프트·설정은 유지하며, 지원되는 URL 필드는 저장 시
`/api/project-media/<id>/<bucket>/<sha256>.<extension>`으로 교체합니다.
원본 바이트를 보존하고, 같은 저장 구역 내 중복 파일은 재사용하고 검증합니다.

브라우저는 서버가 반환한 URL을 현재 노드에 반영하되, 그 사이 발생한 최신 캔버스 수정은 유지합니다.
대기 중인 다음 저장도 교체된 URL을 사용합니다.
최초 업로드는 data URL로 한 번 전송하지만 이후에는 파일 URL을 사용합니다.

Codex·TopView 참조 입력, `describe_media`, 영상 이어붙이기와 영화 조립은
새 URL을 로컬 파일로 해석합니다. HTTP 파일 제공은 바이트 범위 요청을 지원합니다.
기존 `/api/generated/` URL도 계속 사용할 수 있으며,
생성 결과는 다음 프로젝트 저장 시 프로젝트 미디어로 복사됩니다.
노드나 프로젝트를 삭제해도 미디어 파일은 삭제하지 않으므로
다른 노드·컬렉션·타임라인·백업의 참조를 보존합니다.

## 기존 데이터 이전

새 백엔드 최초 시작 시 기존 프로젝트 JSON과 삭제 기록을 DB로 한 번 이전합니다.
원본 JSON·미디어·기존 백업은 삭제하거나 이동하지 않습니다.
이전 완료 표시를 DB에 남겨 재시작 때 오래된 JSON으로 덮어쓰거나 삭제한 프로젝트를 되살리지 않습니다.
손상된 프로젝트 파일은 조용히 누락시키지 않고 시작 오류로 알립니다.
기존 JSON의 revision을 유지하므로 저장 프로토콜과 미디어 URL은 그대로 호환됩니다.

적용 전 서버를 중지하고 `~/.franklin/projects` 전체를 백업하세요.
변경 전 상태는 `.backups`에 보관합니다. DB 전환 후 오래된 JSON 저장소 도구를 운영 데이터에 실행하지 마세요.
DB 변경을 구버전으로 되돌려야 한다면 먼저 현재 API의 전체 프로젝트를 별도 JSON으로 내보내야 합니다.
그냥 옛 코드를 실행하면 이전 당시 JSON만 보여 이후 작업이 누락됩니다.

미디어 분리 메서드 `migrateMedia()`는 DB에 저장된 프로젝트에 적용됩니다.
GET 요청으로 미디어 변환을 실행하지 않으며 공용 생성 원본도 삭제하지 않습니다.

## 부분 변경 API와 동시 작업 범위

`POST /api/projects/patch`는 다음 형식을 받습니다.

```json
{
  "id": "project_id",
  "patch": {
    "baseRevision": 1,
    "nodes": [{ "id": "n1", "before": null, "after": { "id": "n1", "type": "upload", "position": { "x": 0, "y": 0 }, "data": {} } }],
    "edges": []
  }
}
```

추가는 before=null, 삭제는 after=null, 수정은 기존 객체와 변경 객체를 전달합니다.
동일 대상의 기존 값이 달라졌으면 409이며 전체 변경 묶음이 취소됩니다.
서로 다른 대상은 최신 프로젝트를 기반으로 병합합니다. 노드 삭제 시 연결도 정리합니다.
프로젝트 이름은 `name: {before, after}`로 변경합니다.
이 API는 생성을 실행하거나 유료 요청을 재제출하지 않습니다.

현재 UI와 Media Agent는 공통 저장 모듈에서 `/patch`를 사용합니다.
서버가 부분 저장 지원을 알리지 않는 구버전이면 기존 `/save`로 동작합니다.
원격 화면 갱신은 3초 polling 방식이며 공동 커서나 CRDT 기반 문자 단위 공동 편집은 아닙니다.
300ms 동안 로컬 수정을 모아 저장하고, 요청 중 발생한 추가 수정은 응답에 3-way 병합합니다.
동일 노드의 동시 수정은 409로 중단하며 원본 초안은 브라우저 복구본과 메모리에 보관합니다.
복구본을 내보낸 뒤 새로고침하세요. 자동 재제출이나 충돌을 무시한 덮어쓰기는 하지 않습니다.
조회 실패는 동기화 상태에 표시하고, 삭제된 프로젝트를 오래된 화면이 재생성하지 않게 합니다.

선택·dragging·resizing·measured는 노드 변경 비교에서 제외합니다. 실제 width/height는 유지합니다.
노드 ID는 타입 접두사+UUID를 사용하며 기존 ID는 바꾸지 않습니다.
원격 변경을 받으면 전체 화면 스냅샷 기반 Undo 이력을 초기화하여 상대의 작업을 되돌리지 않습니다.
삭제 Undo는 현재 revision을 확인한 요청만 허용하며, 오래된 복원 요청은 차단합니다.
사용자별 권한 및 생성 작업 잠금은 없으므로 같은 노드의 유료 생성은 한 사용자가 담당해야 합니다.

## 목록 조회

Projects 화면은 노드·연결을 제외한 `/api/projects?summary=1`을 주기적으로 조회하고,
선택한 캔버스는 `/api/projects/<id>`로 가져옵니다.
기존 전체 목록 API도 호환성을 위해 유지합니다.
요약 조회는 이미 열린 캔버스의 저장 버전 토큰을 변경하지 않습니다.

## 미디어 사용량과 정리 후보

Projects의 `/api/media/audit`는 조회 전용입니다.
DB의 현재 프로젝트·타임라인 URL, 보존한 이전 JSON·백업 URL과 원본 데이터 해시,
현재 브라우저가 전달한 컬렉션 URL을 확인합니다.
수정 후 30일이 지나지 않은 파일은 후보에서 제외합니다.

다른 브라우저의 동기화되지 않은 참조는 확인할 수 없으므로
후보가 곧 안전하게 삭제 가능한 파일이라는 뜻은 아닙니다.
삭제·이동·정리 API는 없습니다.
임시 작업 폴더와 백업 JSON 용량은 사용량 집계에서 제외됩니다.
참조 JSON을 읽지 못하면 미참조로 보이는 오래된 파일도 정리 후보가 아니라
미검증 상태로 분류합니다.

## 제한사항

미사용 미디어 자동 삭제는 없으며, 기존 브라우저 전용 데이터 복구는 명시적으로 실행해야 합니다.
다른 PC로 옮길 때는 서버를 정지한 뒤 DB/WAL/SHM을 포함한 projects 전체와 미디어 디렉터리,
다른 프로젝트 또는 외부 파일에 대한 참조도 함께 고려해야 합니다.
