# 변경 이력

## 미발행 — 저장소 이름 변경

- GitHub 저장소를 `wildparky/IPOS`로 변경하고 로컬 origin 및 문서·앱 저장소 링크 갱신
- 기존 커밋·이슈·태그·릴리스 유지. 패키지 식별자, 프로젝트·미디어 저장 경로 및 브라우저 저장 키는 유지

## v0.2.0 — 2026-09-18

- 브랜드를 IPOS로 통일하고 OS를 녹색으로 표시, 하단 및 Settings 버전을 0.2.0으로 변경
- 사이드바 Billing 및 상단 Group / Frame 진입 버튼 숨김(설정의 결제 기능과 선택 메뉴의 그룹 기능은 유지)
- 프로젝트 이름을 동기화 상태 앞에 배치하고 글자 크기 20% 확대, 동기화 상태를 영어로 표시
- 마우스 가운데 버튼 팬, 오른쪽 버튼 기존 메뉴, 왼쪽 드래그 부분 교차 다중 선택
- 다중 선택 전용 8개 작업 메뉴 및 그룹 메뉴 스타일 통일
- 그룹/다중 선택 Auto Arrange 공유: 입력 노드는 왼쪽, 후속 노드는 오른쪽, 같은 단계는 수직 정렬
- 드래그 종료 시 그룹 박스 안에 완전히 포함된 노드만 멤버로 유지하며 진입/이탈 반영
- 모든 노드에 Production Status 및 어셋타입 단일 선택 태그, 신규 노드 WIP 기본값 적용
- 상태색 선택 아웃라인 및 원형 S 아이콘 추가. 녹색 체크는 생성/업로드 완료 표시로 별도 유지
- 그룹 상태색 배경, 실선 테두리 및 기존 대비 배경 불투명도 20% 증가
- Media Agent 참조 추가는 입력란에만 반영하며 자동 전송하지 않음
- WebSocket 실시간 협업 및 사용자 인증은 이번 릴리스에 포함하지 않음

## 2026-09-18 — 캔버스 UI 및 그룹 편집

- MOFAC 원본 로고와 IP OS 브랜드 적용, 저장소·복구 메뉴를 Settings → Storage로 이동
- 지갑 안내 배너 제거, 연결 버튼 hover 표시 및 연결선의 카드 경계 정렬
- 생성 이미지의 실제 비율에 맞춘 노드 크기 조정
- 부분 교차 박스 선택과 선택 노드를 감싸는 Group 생성, 멤버 ID 저장 및 함께 이동
- 그룹 Tag·Auto Sort·UnGroup 메뉴와 이름 더블클릭 편집 추가
- UnGroup은 내부 노드를 유지하며 프레임만 제거, 태그·이름·멤버 정보는 기존 노드 저장 경로 사용
- 빌드 및 기존 테스트 통과. 신규 그룹 메뉴의 브라우저 동작 검증은 아직 미완료
- WebSocket·사용자 인증은 이슈 #1의 계획이며 이번 변경에 구현되지 않음

## 미발행 — SQLite 프로젝트 저장소 기반

- 프로젝트·노드·연결을 SQLite로 분리하고 기존 JSON·삭제 기록을 최초 한 번 이전
- 원본 JSON·미디어 URL·기존 백업 보존, WAL 및 원자적 트랜잭션 사용
- DB 요약 조회와 노드·연결별 before/after 부분 변경 API 추가
- 미디어 감사에서 DB 참조를 확인하고 이전 JSON은 백업 참조로 보호
- 기존 Canvas 및 Media Agent 전체 저장·revision 충돌 방지 유지

후속 구현: Canvas·Media Agent의 부분 저장, 저장 중 후속 편집 병합, 3초 원격 polling,
충돌 복구본 보호, UUID 노드 ID, 로컬 선택 유지, 원격 변경 시 Undo 초기화를 추가했습니다.
텍스트·가사 노드의 직접 객체 수정을 제거해 저장과 원격 반영 경로를 거치게 했습니다.
provider 인증·생성 경로는 변경하지 않습니다. 공동 커서·사용자 인증·작업 잠금은 없습니다.

검증: 테스트 50개 중 49개 통과, Windows 심볼릭 링크 권한으로 1개 건너뜀.
typecheck·build 통과(기존 큰 번들 경고 유지). Node SQLite experimental 경고가 있습니다.
운영 JSON 복사본의 프로젝트 2개·노드 36개·연결 13개 이전 및 DB 무결성 검사를 통과했습니다.
최초 재시작 시도는 차단됐으나, 후속 작업에서 JSON 백업 후 운영 백엔드를 재시작해 SQLite를 적용했습니다.
내장 브라우저의 기존 탭 ERR_ABORTED는 새 탭으로 재연결해 정상 표시를 확인했습니다.
별도 테스트 프로젝트에서 텍스트 변경이 서버에 저장되고 다른 탭에 반영되는 것을 확인했습니다.

## 2026-09-18 — fb6ff30

커밋: [Codex·TopView 브리지와 서버 기반 Canvas](https://github.com/wildparky/IPOS/commit/fb6ff30)

- 공식 호스트 기반 Codex 위임과 TopView MCP 영상 실행 경로 추가
- Seedance 모델·모드 설정, 제공자 진행률, 참조 업로드와 결과 파일 가져오기 추가
- 다중 이미지 드롭, 원본 비율 photo 노드, TopView 원본 참조 전송 추가
- 프로젝트 기준 저장소를 버전 검사 방식의 백엔드 파일로 전환하고 브라우저 복구·백업 유지
- 원본 바이트를 삭제하지 않고 JSON 내부 미디어를 해시 기반 프로젝트 파일로 분리
- 경량 프로젝트 요약, 선택한 캔버스 상세 조회, 조회 전용 미디어 사용량 검사 추가
- 내부망 HTTP용 UUID 대체 구현과 개발용 HTTPS 호스트 허용 설정 추가
- 기존 BlockRun 경로와 Canvas·Media Agent 기능 유지

검증: 빌드 성공(큰 번들 경고 유지), 테스트 29개 통과,
Windows 심볼릭 링크 권한 제한으로 1개 건너뜀.

제한사항: 실시간 협업·자동 병합·미디어 자동 삭제·다중 사용자 인증은 없습니다.
일부 상태는 브라우저에만 저장됩니다.
Tailscale Serve는 서버 OS 설정이며 이 커밋만으로 배포되지 않습니다.

## 문서 후속 작업 — c679f3b

설치, 구조, 생성, 저장, 원격 접속, 문제 해결 문서를 docs에 추가하고 README에서 연결했습니다.
실행 동작에는 변경이 없습니다.

## 한국어 문서 정리

변경 이력, 브리지·저장소 상세 문서와 README 문서 안내를 한국어로 통일했습니다.
명령어·API·파일명은 유지했습니다.
